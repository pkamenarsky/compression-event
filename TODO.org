* Low hanging fruit
  (copies; the originals stay where they are)
** TODO remove debug output/controls when game NOT started from editor (editor can pass a debug: true option to game or something that is by default false)
*** fps counter, <, > shader switching (and ; ' sky switching beside it)
** TODO write rotation degrees / scale factor / move distance while transforming
   the amount gestures already set `reading` in the same move handler (canvas/index.ts:811); add labels for T/R/S/X/Y
** TODO spacing in deform a slider; everything in the deform panel -> sliders
   give the deform jitter a higher range
   the fields are all `number(...)` in editor/src/pane.ts:300; add a range input next to or instead of it
** TODO ask yes/no if level unsaved
   keep the world last saved/opened in state, compare by identity, `beforeunload` + confirm on open

* Before the final level file format
** TODO level history
** TODO keyframes in keyframes... can we simplify?
** TODO each level has different wall heights/RenderConfigs
*** even better - different RenderConfig for each version/keyframe
*** sky shader configurable (pasting GLSL into the editor is fine)
** TODO adaptive tables - per polygon instead of smallest event (chain a finer table to the parent?)

* Before the final editor UI (clips of making levels)
** Keyframes & versions
*** TODO a selection should select the births/deaths in the keyframe view as well (and whatever ops are common too?)
*** TODO drag all transforms of a version to next/previous one
*** TODO two tiered keyframe view? the versions + then the keys in a separate view
*** TODO ghost button: none/all/next/subsequent/strip (current version always visible)
    (per-thing ghost switch exists)
**** visible versions as a strip instead of individual lines
** Baking
*** TODO bake for specific versions only (bake button exists)
*** TODO draw red line in timeline at topology events after baking
*** TODO slow motion animation to check for strange frames (drag the arrow in the keyframe strip?)
*** TODO settings: autobake
** Editing tools
*** TODO handles on a shape to scale, rotate and erode — or is this superseded by the T/R/S/E key gestures?
**** exact shape and form
**** created shape can't be dragged (nice to create shapes inside other shapes) — either a handle for transformation or disable dragging; dragging points/edges still works
*** TODO dragging a point scales/rotates uniformly
*** TODO selection of overlapping vertices (right-click lists things under the cursor, but not corners)
*** TODO create button should be selected first (!)
*** TODO mirror something; one-time op? (scale can't go negative)
*** TODO options pane
**** show/hide floor polygons (per-thing hide/lock/solo exist, no per-kind switch)
**** snap to grid
** Look & layout
*** TODO inspector view - all transforms + effects in one pane
**** polygon masks (level + void{solid} etc)
*** TODO write rotation degrees when rotating (and scale factor, move distance); only amounts have a label now
*** TODO statusbar shows action + available modifier keys (it only shows messages now)
*** TODO make it more clear when drilled into group (breadcrumb exists)
*** TODO better UI for unchained poly/groups (Cmd+U / Cmd+Shift+U exist)
*** TODO spacing in deform a slider; everything in the deform panel -> sliders
*** TODO show RenderConfig in the editor, not with \ + ~
*** TODO styling: right click dialog (the "under the cursor" picker)
*** TODO pressing ? overlays tooltips on everything visible
*** TODO ask yes/no if level unsaved
*** TODO layout: statusbar; above it group breadcrumbs; above that baking progress
**** where: bake button, 3d view button, keyframe strip, delink button
** Shortcuts (magit-like sketch)
  c - create shape; cr - rect; cn - n-gon; cv - polyline; ca - artefact; cp - path
    mode in UI; drag - creates shape (or click in case of artefact or click + click + ... in the case of polyline or path)
  e - edit; ep - points; ee - edges; ep - polys (artefacts are always selectable)
    mode in UI; t - translate; r - rotate; s - scale; groups; selection (+selection stack for each element), direct selection;
  e - effects; ec - chamfer; en - noise
    click on poly / group & drag

  v - versions; vn/arrow down (global) - next; vp/arrow up (global) - prev
  bb - bake

  // p - paths; same as in points mode
  // a - artifacts
  // p - points; + - add points; - delete points; click - select; drag - marquee; shift+selection gesture - add to selection
  // t - select; tt - translate; tr - rotate; ts - scale; te - erode; gg/cmd+g - group; gu/cmd-shift-g - ungroup; click - select group; cmd+click - select poly; drag - marquee; shift+selection gesture - add to selection

  questions
    create a rect and want to move/scale it right away? friction
    create an artefact but want to move it? friction
    want to edit an edge? have to click on edge first - friction

* Before clips of the game
** TODO fog when dying so you can't see "behind the walls"
** TODO maybe we don't need the fade
** TODO artefact: "DON'T LOOK BACK"
** TODO artefact: shrink walls for 5sec
** TODO remove debug output
*** fps counter
*** <, > for shader switching

* Later
** sound: dinosaurs? https://www.youtube.com/watch?v=Fqf8EYdCehw
** marketing: https://www.youtube.com/watch?v=90ImoX_e8nE
