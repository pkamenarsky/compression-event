#+TODO: TODO | DONE

* Before the final level file format
** TODO ops should be append-only? e.g. how to repeat the last small translate otherwise?
*** or should repeatable transforms be "effects" as well? and only effects can repeat?
** TODO keyframe can be repeated
** TODO keyframes in keyframes... can we simplify?
** TODO polygon masks (level + void{solid} etc)
** TODO each level has different wall heights/RenderConfigs
*** even better - different RenderConfig for each version/keyframe
*** sky shader configurable (pasting GLSL into the editor is fine)
** TODO adaptive tables - per polygon instead of smallest event (chain a finer table to the parent?)
** TODO effects act on selection: chamfer etc, erosion as well?
** TODO when saving levels, don't overwrite; append
** DONE keyframes: edit, add keyframe stores delta to previous; click a keyframe to edit it
   (implemented as keys; ⌥←/⌥→ step between them)

* Before the final editor UI (clips of making levels)
** Keyframes & versions
*** TODO a selection should select the births/deaths in the keyframe view as well (and whatever ops are common too?)
*** TODO dragging transforms left/right only drags one version
*** TODO two tiered keyframe view? the versions + then the keys in a separate view
*** TODO version view horizontal, like keyframes
**** version management
**** visible versions as a strip instead of individual lines
**** button: none/all/next/subsequent/strip (current version always visible)
** Baking
*** TODO bake button in the version view (also bake for specific versions only)
*** TODO draw red line in timeline at topology events after baking
*** TODO slow motion animation to check for strange frames (drag the arrow in the keyframe strip?)
*** TODO settings: autobake
** Editing tools
*** DONE select polygons / edges / points (one tool, three pickings; shift, marquee)
*** DONE create: rect, n-gon, polyline/path, artefact
*** DONE snap to grid; grid size +/-; five-degree / 45° steps with Alt
*** TODO a shape in both create and select mode has handles to scale, rotate and erode
**** TODO exact shape and form
**** created shape can't be dragged (nice to create shapes inside other shapes) — either a handle for transformation or disable dragging; dragging points/edges still works
*** TODO dragging a point scales/rotates uniformly
*** TODO add/delete points and edges in point/edge picking
*** TODO selection stack needs to be able to hold heterogeneous shapes
*** TODO selection of overlapping vertices
*** TODO create button should be selected first (!)
*** TODO n-gon shows slider for point count (arrow up/down + count)
*** TODO mirror something; one-time op?
** Look & layout
*** TODO remove pkamenarsky itch.io from 3d view in editor
*** TODO statusbar shows action + available modifier keys
*** TODO write rotation degrees when rotating (maybe any transformation), in the n-gon count style
*** TODO make it more clear when drilled into group (breadcrumb exists)
*** TODO better UI for unchained poly/groups
*** TODO show/hide floor polygons
*** TODO spacing in deform a slider; everything in the deform panel -> sliders
*** TODO show RenderConfig in the editor, not with \ + ~
*** TODO styling: right click dialog
*** TODO pressing ? overlays tooltips on everything visible
*** TODO saving levels is cumbersome currently
*** TODO ask yes/no if level unsaved
*** TODO layout: statusbar; above it group breadcrumbs; above that baking progress
**** where: bake button, 3d view button, version strip, delink button
** Shortcut scheme (design note)
   c create: cr rect, cn n-gon, cv polyline, ca artefact, cp path
     drag creates (click for artefact, click+click... for polyline/path)
   e edit: ep points, ee edges, ep polys (artefacts always selectable)
     t translate, r rotate, s scale; groups; selection stack per element
   e effects (clashes with edit): ec chamfer, en noise — click poly/group & drag
   v versions: vn/↓ next, vp/↑ prev
   bb bake
   old: gg/cmd+g group, gu/cmd+shift+g ungroup; cmd+click selects poly; +/- add/delete points
   friction questions:
     create a rect and want to move/scale it right away?
     create an artefact but want to move it?
     want to edit an edge? have to click it first

* Before clips of the game
** TODO fog when dying so you can't see "behind the walls"
** TODO maybe we don't need the fade
** TODO artefact: "DON'T LOOK BACK"
** TODO artefact: shrink walls for 5sec
** TODO remove debug output
*** fps counter
*** <, > for shader switching

* Later
** sound: dinosaurs? https://www.youtube.com/watch?v=Fqf8EYdCehw
** marketing: https://www.youtube.com/watch?v=90ImoX_e8nE
